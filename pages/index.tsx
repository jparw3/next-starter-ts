import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div className="flex h-screen items-center justify-center">
      Next.JS, Tailwindcss, Framer Motion, Typescript, Husky
    </div>
  );
};

export default Home;
